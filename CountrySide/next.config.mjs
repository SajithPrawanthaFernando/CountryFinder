/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "aceternity.com",
      "images.unsplash.com",
      "flagcdn.com",
      "upload.wikimedia.org",
    ],
  },
};

export default nextConfig;
